.runman-member.J-runman-member(ng-repeat="mebmer in memberCtrl.members track by $index")
	.runman-way
		span.runman-name {{mebmer.name}}
		//- span.runman-score {{mebmer.score}}
	div(class="runman-doll {{mebmer.gender}}" wq-runman-doll index="{{$index}}")

//- style="left: {{mebmer.score | rundistance:memberCtrl.curtimes:memberCtrl.totaltimes:memberCtrl.fulllength}}"

